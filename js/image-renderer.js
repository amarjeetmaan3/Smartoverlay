export class ImageRenderer{constructor(container){this.container=container;this.nodes=new Map()}render(box){let el=this.nodes.get(box.id);if(!el){el=document.createElement("img");el.className="so-media-box so-image-box";el.dataset.boxId=box.id;this.container.appendChild(el);this.nodes.set(box.id,el)}el.src=box.url||"";el.alt=box.alt||box.name||"Image";el.style.left=(box.x||0)+"px";el.style.top=(box.y||0)+"px";el.style.width=(box.width||400)+"px";el.style.height=(box.height||250)+"px";el.style.objectFit=box.fit||"contain";el.style.opacity=box.opacity??1;el.style.zIndex=box.zIndex||1;el.style.transform=`rotate(${Number(box.rotation)||0}deg)`;return el}remove(id){const el=this.nodes.get(id);if(el)el.remove();this.nodes.delete(id)}}export default ImageRenderer;