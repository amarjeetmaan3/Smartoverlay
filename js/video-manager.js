export class VideoManager{constructor(){this.videos=new Map()}add(data){const id=data.id||`video_${Date.now()}_${Math.random().toString(36).slice(2,7)}`;const item={id,type:"video",url:"",autoplay:true,muted:true,loop:true,controls:false,fit:"contain",...data};this.videos.set(id,item);return item}get(id){return this.videos.get(id)}update(id,data){const item=this.get(id);if(!item)return null;Object.assign(item,data);return item}remove(id){return this.videos.delete(id)}all(){return [...this.videos.values()]}}export default VideoManager;