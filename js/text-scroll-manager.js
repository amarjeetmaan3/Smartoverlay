export class TextScrollManager{constructor(){this.states=new Map()}configure(id,options={}){const state={direction:"right-to-left",speed:50,delay:0,loop:true,repeatCount:0,startPosition:0,distance:100,paused:false,...options};this.states.set(id,state);return state}get(id){return this.states.get(id)}update(id,data){const s=this.get(id);if(!s)return null;Object.assign(s,data);return s}pause(id){const s=this.get(id);if(s)s.paused=true;return s}resume(id){const s=this.get(id);if(s)s.paused=false;return s}reset(id){const s=this.get(id);if(s)s.startPosition=0;return s}}export default TextScrollManager;