import QuestionStore from "./question-store.js";import QuestionSelector from "./question-selector.js";export class QuestionBankV2{constructor(){this.store=new QuestionStore();this.selector=new QuestionSelector()}load(items=[]){this.store.setAll(items);this.selector.setPool(this.store.all());return this.store.all()}add(q){const x=this.store.add(q);this.selector.setPool(this.store.all());return x}get(id){return this.store.get(id)}update(id,d){return this.store.update(id,d)}remove(id){const ok=this.store.remove(id);this.selector.setPool(this.store.all());return ok}select(id){return this.selector.select(id)}selectRange(a,b){return this.selector.range(a,b)}random(n){return this.selector.random(n)}all(){return this.store.all()}export(){return this.store.export()}}export default QuestionBankV2;