export class MediaBoxManager{constructor(){this.items=new Map()}create(data={}){const id=data.id||`media_${Date.now()}_${Math.random().toString(36).slice(2,7)}`;const item={id,type:"media",x:0,y:0,width:400,height:250,rotation:0,opacity:1,zIndex:1,visible:true,locked:false,fit:"contain",mask:"none",...data};this.items.set(id,item);return item}get(id){return this.items.get(id)}update(id,data){const item=this.get(id);if(!item)return null;Object.assign(item,data);return item}remove(id){return this.items.delete(id)}all(){return [...this.items.values()]}export(){return this.all().map(x=>({...x}))}import(items=[]){this.items.clear();items.forEach(x=>this.items.set(x.id,{...x}));return this.all()}}export default MediaBoxManager;