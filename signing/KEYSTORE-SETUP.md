# SmartOverlay Release Keystore Setup

The release keystore must be created once and preserved permanently.

Important:

- Never commit the real `.jks` file to GitHub.
- Never commit passwords to GitHub.
- Never change the release signing key for an existing app.
- Keep a secure offline backup of the keystore.

Recommended alias:

smartoverlay

The same keystore must be used for every future APK update.
