export class TextRenderer{constructor(container){this.container=container;this.nodes=new Map()}render(box){let el=this.nodes.get(box.id);if(!el){el=document.createElement("div");el.className="so-box so-text";el.dataset.boxId=box.id;this.container.appendChild(el);this.nodes.set(box.id,el)}el.textContent=box.content??"";el.style.fontFamily=box.fontFamily||"Arial";el.style.fontSize=(Number(box.fontSize)||48)+"px";el.style.fontWeight=box.fontWeight||400;el.style.fontStyle=box.fontStyle||"normal";el.style.lineHeight=box.lineHeight||1.15;el.style.letterSpacing=(Number(box.letterSpacing)||0)+"px";el.style.wordSpacing=(Number(box.wordSpacing)||0)+"px";el.style.color=box.color||"#fff";el.style.textAlign=box.textAlign||"left";el.style.opacity=box.opacity??1;el.style.left=(box.x||0)+"px";el.style.top=(box.y||0)+"px";el.style.width=(box.width||300)+"px";el.style.height=(box.height||100)+"px";el.style.zIndex=box.zIndex||1;el.style.transform=`rotate(${Number(box.rotation)||0}deg)`;return el}remove(id){const el=this.nodes.get(id);if(el)el.remove();this.nodes.delete(id)}clear(){this.nodes.forEach(x=>x.remove());this.nodes.clear()}}export default TextRenderer;