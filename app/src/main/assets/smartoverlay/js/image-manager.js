export class ImageManager{constructor(){this.images=new Map()}add(data){const id=data.id||`image_${Date.now()}_${Math.random().toString(36).slice(2,7)}`;const item={id,type:"image",url:"",name:"",...data};this.images.set(id,item);return item}get(id){return this.images.get(id)}update(id,data){const item=this.get(id);if(!item)return null;Object.assign(item,data);return item}remove(id){return this.images.delete(id)}all(){return [...this.images.values()]}}export default ImageManager;