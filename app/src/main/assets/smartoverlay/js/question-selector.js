export class QuestionSelector{constructor(){this.pool=[];this.selected=[]}setPool(a=[]){this.pool=[...a];this.selected=[]}select(id){const q=this.pool.find(x=>x.id===id);this.selected=q?[q]:[];return q||null}range(a,b){a=Number(a)||1;b=Number(b)||a;this.selected=this.pool.filter(q=>Number(q.No)>=Math.min(a,b)&&Number(q.No)<=Math.max(a,b));return this.selected}random(n=1){n=Math.max(1,Math.min(this.pool.length,Number(n)||1));this.selected=[...this.pool].sort(()=>Math.random()-.5).slice(0,n);return this.selected}getSelected(){return[...this.selected]}}export default QuestionSelector;